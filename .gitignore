# === Terraform local directories ===
**/.terraform/

# === Terraform state files ===
*.tfstate
*.tfstate.*

# === Terraform variable files (sensitive) ===
*.tfvars
*.tfvars.json

# === Terraform override files ===
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# === Terraform plan files ===
*.tfplan

# === Crash logs ===
crash.log
crash.*.log

# === Terraform CLI config ===
.terraformrc
terraform.rc

# === SSH keys or secrets ===
keys/
config/

# === Optional: ignore dotenv, local secrets, etc. ===
.env
.env.*
secrets.*
.workspace
